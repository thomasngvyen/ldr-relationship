/** Shared media queries — keep in sync with CSS breakpoints */

export const MQ = {
  /** Visit cards, moods grid, countdown */
  mobile: '(max-width: 540px)',
  /** Date idea board columns collapse */
  tablet: '(max-width: 860px)',
  /** Staggered menu full-width panel */
  menuCollapse: '(max-width: 1024px)',
  /** Respect OS “reduce motion” setting */
  reducedMotion: '(prefers-reduced-motion: reduce)',
}
