# Leave empty for local preview (requests go to /api and Vite proxies to the server).
# For a real deploy where the API is on another host, set that URL at build time, e.g.:
# VITE_API_URL=https://your-api.onrender.com
VITE_API_URL=
