# Copied into the client bundle at build time (only vars prefixed with VITE_).
#
# Local preview / same-origin hosting: leave empty so fetch('/api/...') is used.
VITE_API_URL=
#
# Separate API host (e.g. Render):
# VITE_API_URL=https://your-api.onrender.com
